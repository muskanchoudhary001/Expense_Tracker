import { styled } from '@mui/system';

const RadioGroup = styled('div')({
  display: 'flex',
  justifyContent: 'center',
  marginBottom: '-10px',
});

const Button = styled('button')({
  marginTop: '20px',
});

export { RadioGroup, Button };
